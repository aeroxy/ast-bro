import * as api from './base';

/** `api` is a module alias, not a package: the qualifier cannot be
 *  compared against a declaration that knows no namespace. */
export class Child implements api.Base {}
