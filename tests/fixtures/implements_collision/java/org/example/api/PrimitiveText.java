package org.example.api;

/**
 * Extends the interface declared in this package. Nothing here reaches
 * BinaryCodec, so this type must stay out of that closure.
 */
public interface PrimitiveText extends TextCodec {
}
